spring.application.name=Billing_software

# Render injects PORT automatically
server.port=${PORT:8080}

# ===============================
# DATABASE CONFIGURATION
# ===============================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d7sr79cm0tmc73df83n0-a.singapore-postgres.render.com:5432/footware_billing_db?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:footware_billing_db_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ovixRrIBRoP0vyxktntrfLqozrU7YTfr}
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / HIBERNATE CONFIGURATION
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=true
spring.jpa.open-in-view=false